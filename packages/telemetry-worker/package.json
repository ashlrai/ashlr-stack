{
  "name": "@ashlr/stack-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that accepts opt-in usage telemetry from the Stack CLI.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
