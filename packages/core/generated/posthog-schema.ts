// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: posthog provision schema (PostHog Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from PostHog API key verification */
export interface PosthogProvisionResponse {
  /** Account id or 'default' */
  id: string;
  /** Account display name or email */
  displayName: string;
  /** Account email */
  email?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Account id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name or email",
      "minLength": 1
    },
    "email": {
      "type": "string",
      "description": "Account email"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw posthog provision API response.
 * Returns violations (empty array = valid).
 */
export function validatePosthogProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → PosthogProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertPosthogProvisionResponse(raw: unknown): PosthogProvisionResponse {
  const violations = validatePosthogProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`posthog provision response validation failed: ${msg}`);
  }
  return raw as PosthogProvisionResponse;
}
