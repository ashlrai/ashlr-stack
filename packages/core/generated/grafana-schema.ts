// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: grafana provision schema (Grafana Cloud Stack)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Grafana Cloud API key verification */
export interface GrafanaProvisionResponse {
  /** Stack/org id or 'default' */
  id: string;
  /** Stack display name */
  displayName: string;
  /** Grafana Cloud org slug */
  orgSlug?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stack/org id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Stack display name",
      "minLength": 1
    },
    "orgSlug": {
      "type": "string",
      "description": "Grafana Cloud org slug"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw grafana provision API response.
 * Returns violations (empty array = valid).
 */
export function validateGrafanaProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → GrafanaProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertGrafanaProvisionResponse(raw: unknown): GrafanaProvisionResponse {
  const violations = validateGrafanaProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`grafana provision response validation failed: ${msg}`);
  }
  return raw as GrafanaProvisionResponse;
}
