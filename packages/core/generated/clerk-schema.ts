// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: clerk provision schema (Clerk Application)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Clerk API key verification */
export interface ClerkProvisionResponse {
  /** Clerk application id or 'default' */
  id: string;
  /** Application display name */
  displayName: string;
  /** Clerk application id */
  application_id?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Clerk application id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Application display name",
      "minLength": 1
    },
    "application_id": {
      "type": "string",
      "description": "Clerk application id"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw clerk provision API response.
 * Returns violations (empty array = valid).
 */
export function validateClerkProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → ClerkProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertClerkProvisionResponse(raw: unknown): ClerkProvisionResponse {
  const violations = validateClerkProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`clerk provision response validation failed: ${msg}`);
  }
  return raw as ClerkProvisionResponse;
}
