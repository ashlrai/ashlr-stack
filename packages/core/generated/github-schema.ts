// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: github provision schema (GitHub User / Org)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from GET /user (identity verification) */
export interface GithubProvisionResponse {
  /** GitHub username/handle */
  login: string;
  /** GitHub numeric user id */
  id: number;
  /** GraphQL node id */
  node_id?: string;
  /** Display name */
  name?: string | null;
  /** Public email */
  email?: string | null;
  /** Company affiliation */
  company?: string | null;
  /** User | Organization | Bot */
  type?: "User" | "Organization" | "Bot";
  /** Profile URL */
  html_url?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "login",
    "id"
  ],
  "properties": {
    "login": {
      "type": "string",
      "description": "GitHub username/handle",
      "minLength": 1
    },
    "id": {
      "type": "integer",
      "description": "GitHub numeric user id",
      "minimum": 1
    },
    "node_id": {
      "type": "string",
      "description": "GraphQL node id"
    },
    "name": {
      "type": [
        "string",
        "null"
      ],
      "description": "Display name",
      "nullable": true
    },
    "email": {
      "type": [
        "string",
        "null"
      ],
      "description": "Public email",
      "nullable": true
    },
    "company": {
      "type": [
        "string",
        "null"
      ],
      "description": "Company affiliation",
      "nullable": true
    },
    "type": {
      "type": "string",
      "description": "User | Organization | Bot",
      "enum": [
        "User",
        "Organization",
        "Bot"
      ]
    },
    "html_url": {
      "type": "string",
      "description": "Profile URL"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw github provision API response.
 * Returns violations (empty array = valid).
 */
export function validateGithubProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → GithubProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertGithubProvisionResponse(raw: unknown): GithubProvisionResponse {
  const violations = validateGithubProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`github provision response validation failed: ${msg}`);
  }
  return raw as GithubProvisionResponse;
}
