// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: vercel provision schema (Vercel Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from POST /v10/projects */
export interface VercelProvisionResponse {
  /** Vercel project id (prj_…) */
  id: string;
  /** Project slug / display name */
  name: string;
  /** Owning account id */
  accountId?: string;
  /** Detected framework (nextjs, vite, etc.) */
  framework?: string | null;
  /** Git repository linkage */
  link?: { type?: string; repo?: string } | null;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "name"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Vercel project id (prj_…)",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Project slug / display name",
      "minLength": 1
    },
    "accountId": {
      "type": "string",
      "description": "Owning account id"
    },
    "framework": {
      "type": [
        "string",
        "null"
      ],
      "description": "Detected framework (nextjs, vite, etc.)",
      "nullable": true
    },
    "link": {
      "type": [
        "object",
        "null"
      ],
      "description": "Git repository linkage",
      "nullable": true,
      "properties": {
        "type": {
          "type": "string"
        },
        "repo": {
          "type": "string"
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw vercel provision API response.
 * Returns violations (empty array = valid).
 */
export function validateVercelProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → VercelProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertVercelProvisionResponse(raw: unknown): VercelProvisionResponse {
  const violations = validateVercelProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`vercel provision response validation failed: ${msg}`);
  }
  return raw as VercelProvisionResponse;
}
