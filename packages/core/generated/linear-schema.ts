// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: linear provision schema (Linear Workspace)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Linear API key verification */
export interface LinearProvisionResponse {
  /** Linear team/user id or 'default' */
  id: string;
  /** Workspace display name or email */
  displayName: string;
  /** Account email */
  email?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Linear team/user id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Workspace display name or email",
      "minLength": 1
    },
    "email": {
      "type": "string",
      "description": "Account email"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw linear provision API response.
 * Returns violations (empty array = valid).
 */
export function validateLinearProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → LinearProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertLinearProvisionResponse(raw: unknown): LinearProvisionResponse {
  const violations = validateLinearProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`linear provision response validation failed: ${msg}`);
  }
  return raw as LinearProvisionResponse;
}
