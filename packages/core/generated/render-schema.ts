// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: render provision schema (Render Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Render API key verification (/v1/owners) */
export interface RenderProvisionResponse {
  /** Render owner id */
  id: string;
  /** Owner display name */
  displayName: string;
  /** Owner name */
  name?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Render owner id",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Owner display name",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Owner name"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw render provision API response.
 * Returns violations (empty array = valid).
 */
export function validateRenderProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → RenderProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertRenderProvisionResponse(raw: unknown): RenderProvisionResponse {
  const violations = validateRenderProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`render provision response validation failed: ${msg}`);
  }
  return raw as RenderProvisionResponse;
}
