// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: aws provision schema (AWS Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from AWS credentials verification (STS GetCallerIdentity) */
export interface AwsProvisionResponse {
  /** AWS account id or user id */
  id: string;
  /** Account display name or ARN */
  displayName: string;
  /** AWS account id (12-digit) */
  account_id?: string;
  /** Caller ARN */
  arn?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "AWS account id or user id",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name or ARN",
      "minLength": 1
    },
    "account_id": {
      "type": "string",
      "description": "AWS account id (12-digit)"
    },
    "arn": {
      "type": "string",
      "description": "Caller ARN"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw aws provision API response.
 * Returns violations (empty array = valid).
 */
export function validateAwsProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → AwsProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertAwsProvisionResponse(raw: unknown): AwsProvisionResponse {
  const violations = validateAwsProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`aws provision response validation failed: ${msg}`);
  }
  return raw as AwsProvisionResponse;
}
