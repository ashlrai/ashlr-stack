// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: mixpanel provision schema (Mixpanel Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Mixpanel API key verification */
export interface MixpanelProvisionResponse {
  /** Project id or 'default' */
  id: string;
  /** Project display name */
  displayName: string;
  /** Mixpanel project id */
  project_id?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Project id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Project display name",
      "minLength": 1
    },
    "project_id": {
      "type": "string",
      "description": "Mixpanel project id"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw mixpanel provision API response.
 * Returns violations (empty array = valid).
 */
export function validateMixpanelProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → MixpanelProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertMixpanelProvisionResponse(raw: unknown): MixpanelProvisionResponse {
  const violations = validateMixpanelProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`mixpanel provision response validation failed: ${msg}`);
  }
  return raw as MixpanelProvisionResponse;
}
