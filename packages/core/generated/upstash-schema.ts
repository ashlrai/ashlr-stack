// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: upstash provision schema (Upstash Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Upstash API key verification */
export interface UpstashProvisionResponse {
  /** Upstash account id or 'default' */
  id: string;
  /** Account name or email */
  displayName: string;
  /** Account email */
  email?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Upstash account id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account name or email",
      "minLength": 1
    },
    "email": {
      "type": "string",
      "description": "Account email"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw upstash provision API response.
 * Returns violations (empty array = valid).
 */
export function validateUpstashProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → UpstashProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertUpstashProvisionResponse(raw: unknown): UpstashProvisionResponse {
  const violations = validateUpstashProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`upstash provision response validation failed: ${msg}`);
  }
  return raw as UpstashProvisionResponse;
}
