// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: convex provision schema (Convex Deployment)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource returned from Convex CLI-based provision */
export interface ConvexProvisionResponse {
  /** Convex deployment id or slug */
  id: string;
  /** Human-readable deployment name */
  displayName: string;
  /** URL slug */
  slug?: string;
  /** Deployment region */
  region?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Convex deployment id or slug",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Human-readable deployment name",
      "minLength": 1
    },
    "slug": {
      "type": "string",
      "description": "URL slug"
    },
    "region": {
      "type": "string",
      "description": "Deployment region"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw convex provision API response.
 * Returns violations (empty array = valid).
 */
export function validateConvexProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → ConvexProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertConvexProvisionResponse(raw: unknown): ConvexProvisionResponse {
  const violations = validateConvexProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`convex provision response validation failed: ${msg}`);
  }
  return raw as ConvexProvisionResponse;
}
