// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: deepseek provision schema (DeepSeek Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from DeepSeek API key verification */
export interface DeepseekProvisionResponse {
  /** Account id or 'default' */
  id: string;
  /** Account display name */
  displayName: string;
  /** Number of available models */
  models?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Account id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name",
      "minLength": 1
    },
    "models": {
      "type": "string",
      "description": "Number of available models"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw deepseek provision API response.
 * Returns violations (empty array = valid).
 */
export function validateDeepseekProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → DeepseekProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertDeepseekProvisionResponse(raw: unknown): DeepseekProvisionResponse {
  const violations = validateDeepseekProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`deepseek provision response validation failed: ${msg}`);
  }
  return raw as DeepseekProvisionResponse;
}
