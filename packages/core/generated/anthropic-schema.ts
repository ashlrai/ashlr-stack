// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: anthropic provision schema (Anthropic Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Anthropic API key verification (/v1/models) */
export interface AnthropicProvisionResponse {
  /** Account id or 'default' */
  id: string;
  /** Account display name */
  displayName: string;
  /** Number of available models */
  models?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Account id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name",
      "minLength": 1
    },
    "models": {
      "type": "string",
      "description": "Number of available models"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw anthropic provision API response.
 * Returns violations (empty array = valid).
 */
export function validateAnthropicProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → AnthropicProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertAnthropicProvisionResponse(raw: unknown): AnthropicProvisionResponse {
  const violations = validateAnthropicProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`anthropic provision response validation failed: ${msg}`);
  }
  return raw as AnthropicProvisionResponse;
}
