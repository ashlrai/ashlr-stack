// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: stripe provision schema (Stripe Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from GET /v1/account (identity verification) */
export interface StripeProvisionResponse {
  /** Stripe account id (acct_… or 'live'/'test' for the default account) */
  id: string;
  /** Business display name */
  display_name?: string | null;
  /** Account email */
  email?: string | null;
  /** ISO 3166-1 alpha-2 country code */
  country?: string | null;
  /** individual | company | non_profit | government_entity */
  business_type?: string | null;
  /** Whether the account can accept charges */
  charges_enabled?: boolean;
}

const _schema = {
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stripe account id (acct_… or 'live'/'test' for the default account)",
      "minLength": 1
    },
    "display_name": {
      "type": [
        "string",
        "null"
      ],
      "description": "Business display name",
      "nullable": true
    },
    "email": {
      "type": [
        "string",
        "null"
      ],
      "description": "Account email",
      "nullable": true
    },
    "country": {
      "type": [
        "string",
        "null"
      ],
      "description": "ISO 3166-1 alpha-2 country code",
      "nullable": true
    },
    "business_type": {
      "type": [
        "string",
        "null"
      ],
      "description": "individual | company | non_profit | government_entity",
      "nullable": true
    },
    "charges_enabled": {
      "type": "boolean",
      "description": "Whether the account can accept charges"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw stripe provision API response.
 * Returns violations (empty array = valid).
 */
export function validateStripeProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → StripeProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertStripeProvisionResponse(raw: unknown): StripeProvisionResponse {
  const violations = validateStripeProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`stripe provision response validation failed: ${msg}`);
  }
  return raw as StripeProvisionResponse;
}
