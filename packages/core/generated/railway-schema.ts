// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: railway provision schema (Railway Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Railway API key verification (me.id) */
export interface RailwayProvisionResponse {
  /** Railway user/team id */
  id: string;
  /** Display name (email or Railway displayName) */
  displayName: string;
  /** Account email */
  email?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Railway user/team id",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Display name (email or Railway displayName)",
      "minLength": 1
    },
    "email": {
      "type": "string",
      "description": "Account email"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw railway provision API response.
 * Returns violations (empty array = valid).
 */
export function validateRailwayProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → RailwayProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertRailwayProvisionResponse(raw: unknown): RailwayProvisionResponse {
  const violations = validateRailwayProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`railway provision response validation failed: ${msg}`);
  }
  return raw as RailwayProvisionResponse;
}
