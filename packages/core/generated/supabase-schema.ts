// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: supabase provision schema (Supabase Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from POST /v1/projects */
export interface SupabaseProvisionResponse {
  /** Supabase project ref (short alphanumeric id) */
  id: string;
  /** Human-readable project name */
  name: string;
  /** AWS region slug (e.g. us-east-1) */
  region?: string;
  /** Owning organization id */
  organization_id?: string;
  /** Project status (ACTIVE_HEALTHY, COMING_UP, etc.) */
  status?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "name"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Supabase project ref (short alphanumeric id)",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Human-readable project name",
      "minLength": 1
    },
    "region": {
      "type": "string",
      "description": "AWS region slug (e.g. us-east-1)"
    },
    "organization_id": {
      "type": "string",
      "description": "Owning organization id"
    },
    "status": {
      "type": "string",
      "description": "Project status (ACTIVE_HEALTHY, COMING_UP, etc.)"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw supabase provision API response.
 * Returns violations (empty array = valid).
 */
export function validateSupabaseProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → SupabaseProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertSupabaseProvisionResponse(raw: unknown): SupabaseProvisionResponse {
  const violations = validateSupabaseProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`supabase provision response validation failed: ${msg}`);
  }
  return raw as SupabaseProvisionResponse;
}
