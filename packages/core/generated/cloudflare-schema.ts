// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: cloudflare provision schema (Cloudflare Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from GET /client/v4/user or accounts */
export interface CloudflareProvisionResponse {
  /** Cloudflare account/user id */
  id: string;
  /** Account name or email */
  displayName: string;
  /** Account email */
  email?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Cloudflare account/user id",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account name or email",
      "minLength": 1
    },
    "email": {
      "type": "string",
      "description": "Account email"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw cloudflare provision API response.
 * Returns violations (empty array = valid).
 */
export function validateCloudflareProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → CloudflareProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertCloudflareProvisionResponse(raw: unknown): CloudflareProvisionResponse {
  const violations = validateCloudflareProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`cloudflare provision response validation failed: ${msg}`);
  }
  return raw as CloudflareProvisionResponse;
}
