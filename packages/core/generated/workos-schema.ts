// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: workos provision schema (WorkOS Application)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from WorkOS API key verification */
export interface WorkosProvisionResponse {
  /** Application id or 'default' */
  id: string;
  /** Application display name */
  displayName: string;
  /** WorkOS environment id */
  environment_id?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Application id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Application display name",
      "minLength": 1
    },
    "environment_id": {
      "type": "string",
      "description": "WorkOS environment id"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw workos provision API response.
 * Returns violations (empty array = valid).
 */
export function validateWorkosProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → WorkosProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertWorkosProvisionResponse(raw: unknown): WorkosProvisionResponse {
  const violations = validateWorkosProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`workos provision response validation failed: ${msg}`);
  }
  return raw as WorkosProvisionResponse;
}
