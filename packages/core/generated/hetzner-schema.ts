// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: hetzner provision schema (Hetzner Cloud Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Hetzner Cloud API key verification */
export interface HetznerProvisionResponse {
  /** Hetzner project id or 'default' */
  id: string;
  /** Project display name */
  displayName: string;
  /** Primary datacenter */
  datacenter?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Hetzner project id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Project display name",
      "minLength": 1
    },
    "datacenter": {
      "type": "string",
      "description": "Primary datacenter"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw hetzner provision API response.
 * Returns violations (empty array = valid).
 */
export function validateHetznerProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → HetznerProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertHetznerProvisionResponse(raw: unknown): HetznerProvisionResponse {
  const violations = validateHetznerProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`hetzner provision response validation failed: ${msg}`);
  }
  return raw as HetznerProvisionResponse;
}
