// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: datadog provision schema (Datadog Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Datadog API key verification */
export interface DatadogProvisionResponse {
  /** Datadog org id or 'default' */
  id: string;
  /** Org display name */
  displayName: string;
  /** Datadog site (datadoghq.com, etc.) */
  site?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Datadog org id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Org display name",
      "minLength": 1
    },
    "site": {
      "type": "string",
      "description": "Datadog site (datadoghq.com, etc.)"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw datadog provision API response.
 * Returns violations (empty array = valid).
 */
export function validateDatadogProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → DatadogProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertDatadogProvisionResponse(raw: unknown): DatadogProvisionResponse {
  const violations = validateDatadogProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`datadog provision response validation failed: ${msg}`);
  }
  return raw as DatadogProvisionResponse;
}
