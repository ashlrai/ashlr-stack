// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: firebase provision schema (Firebase Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Firebase/GCP CLI-based provision */
export interface FirebaseProvisionResponse {
  /** Firebase project id or number */
  id: string;
  /** Project display name */
  displayName: string;
  /** Firebase project id slug */
  projectId?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Firebase project id or number",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Project display name",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "description": "Firebase project id slug"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw firebase provision API response.
 * Returns violations (empty array = valid).
 */
export function validateFirebaseProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → FirebaseProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertFirebaseProvisionResponse(raw: unknown): FirebaseProvisionResponse {
  const violations = validateFirebaseProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`firebase provision response validation failed: ${msg}`);
  }
  return raw as FirebaseProvisionResponse;
}
