// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: turso provision schema (Turso Database)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from POST /v1/organizations/:org/databases */
export interface TursoProvisionResponse {
  /** Database name */
  name: string;
  /** Primary region slug */
  primary_region?: string;
  /** Group the database belongs to */
  group?: string;
  /** Database type (logical, etc.) */
  type?: string;
  /** Database hostname */
  hostname?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Database name",
      "minLength": 1
    },
    "primary_region": {
      "type": "string",
      "description": "Primary region slug"
    },
    "group": {
      "type": "string",
      "description": "Group the database belongs to"
    },
    "type": {
      "type": "string",
      "description": "Database type (logical, etc.)"
    },
    "hostname": {
      "type": "string",
      "description": "Database hostname"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw turso provision API response.
 * Returns violations (empty array = valid).
 */
export function validateTursoProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → TursoProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertTursoProvisionResponse(raw: unknown): TursoProvisionResponse {
  const violations = validateTursoProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`turso provision response validation failed: ${msg}`);
  }
  return raw as TursoProvisionResponse;
}
