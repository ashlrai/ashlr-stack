// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: gcp provision schema (GCP Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from GCP credentials verification */
export interface GcpProvisionResponse {
  /** GCP project id or 'default' */
  id: string;
  /** Project display name */
  displayName: string;
  /** GCP project id */
  project_id?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "GCP project id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Project display name",
      "minLength": 1
    },
    "project_id": {
      "type": "string",
      "description": "GCP project id"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw gcp provision API response.
 * Returns violations (empty array = valid).
 */
export function validateGcpProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → GcpProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertGcpProvisionResponse(raw: unknown): GcpProvisionResponse {
  const violations = validateGcpProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`gcp provision response validation failed: ${msg}`);
  }
  return raw as GcpProvisionResponse;
}
