// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: replicate provision schema (Replicate Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Replicate API key verification */
export interface ReplicateProvisionResponse {
  /** Account id or username */
  id: string;
  /** Account display name */
  displayName: string;
  /** Replicate username */
  username?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Account id or username",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name",
      "minLength": 1
    },
    "username": {
      "type": "string",
      "description": "Replicate username"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw replicate provision API response.
 * Returns violations (empty array = valid).
 */
export function validateReplicateProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → ReplicateProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertReplicateProvisionResponse(raw: unknown): ReplicateProvisionResponse {
  const violations = validateReplicateProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`replicate provision response validation failed: ${msg}`);
  }
  return raw as ReplicateProvisionResponse;
}
