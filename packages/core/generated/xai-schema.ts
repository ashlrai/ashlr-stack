// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: xai provision schema (xAI Account)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from xAI API key verification */
export interface XaiProvisionResponse {
  /** Account id or 'default' */
  id: string;
  /** Account display name */
  displayName: string;
  /** Number of available models */
  models?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Account id or 'default'",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Account display name",
      "minLength": 1
    },
    "models": {
      "type": "string",
      "description": "Number of available models"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw xai provision API response.
 * Returns violations (empty array = valid).
 */
export function validateXaiProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → XaiProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertXaiProvisionResponse(raw: unknown): XaiProvisionResponse {
  const violations = validateXaiProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`xai provision response validation failed: ${msg}`);
  }
  return raw as XaiProvisionResponse;
}
