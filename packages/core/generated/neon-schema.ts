// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: neon provision schema (Neon Project)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Response from POST /v2/projects (body.project) */
export interface NeonProvisionResponse {
  /** Neon project id */
  id: string;
  /** Human-readable project name */
  name: string;
  /** Region (e.g. aws-us-east-2) */
  region_id?: string;
  /** PostgreSQL major version */
  pg_version?: number;
  /** ISO 8601 creation timestamp */
  created_at?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "name"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Neon project id",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Human-readable project name",
      "minLength": 1
    },
    "region_id": {
      "type": "string",
      "description": "Region (e.g. aws-us-east-2)"
    },
    "pg_version": {
      "type": "integer",
      "description": "PostgreSQL major version",
      "minimum": 14
    },
    "created_at": {
      "type": "string",
      "description": "ISO 8601 creation timestamp"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw neon provision API response.
 * Returns violations (empty array = valid).
 */
export function validateNeonProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → NeonProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertNeonProvisionResponse(raw: unknown): NeonProvisionResponse {
  const violations = validateNeonProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`neon provision response validation failed: ${msg}`);
  }
  return raw as NeonProvisionResponse;
}
