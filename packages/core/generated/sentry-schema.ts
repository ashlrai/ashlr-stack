// AUTO-GENERATED by provision-schema codegen — do not edit by hand
// Source: sentry provision schema (Sentry Organization)

import { validateSchema, type SchemaViolation } from "../provision-schema.ts";

/** Synthetic resource from Sentry API key verification */
export interface SentryProvisionResponse {
  /** Sentry org id or slug */
  id: string;
  /** Org display name */
  displayName: string;
  /** Sentry org slug */
  org_slug?: string;
}

const _schema = {
  "type": "object",
  "required": [
    "id",
    "displayName"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Sentry org id or slug",
      "minLength": 1
    },
    "displayName": {
      "type": "string",
      "description": "Org display name",
      "minLength": 1
    },
    "org_slug": {
      "type": "string",
      "description": "Sentry org slug"
    }
  },
  "additionalProperties": true
} as const;

/**
 * Validate a raw sentry provision API response.
 * Returns violations (empty array = valid).
 */
export function validateSentryProvisionResponse(raw: unknown): SchemaViolation[] {
  // biome-ignore lint/suspicious/noExplicitAny: schema cast
  return validateSchema(raw, _schema as any);
}

/**
 * Type-guard: narrow unknown → SentryProvisionResponse.
 * Throws on invalid response so callers get a clear error at provision-time.
 */
export function assertSentryProvisionResponse(raw: unknown): SentryProvisionResponse {
  const violations = validateSentryProvisionResponse(raw);
  if (violations.length > 0) {
    const msg = violations.map(v => `${v.path}: ${v.message}`).join("; ");
    throw new Error(`sentry provision response validation failed: ${msg}`);
  }
  return raw as SentryProvisionResponse;
}
