---
description: Show a one-screen overview of the current stack.
---

Call `stack_status`. If `.stack.toml` is missing, offer to run `stack_init`. If Phantom is not installed, give the install hint.
